"use client";
import{useEffect,useState}from"react";import Link from"next/link";import{ArrowUpRight,BookOpen}from"lucide-react";import type{Course}from"@/types/lms";
const API=process.env.NEXT_PUBLIC_API_URL||"http://127.0.0.1:8000";
export default function PublicAcademyCatalog(){const[courses,setCourses]=useState<Course[]>([]);const[loading,setLoading]=useState(true);useEffect(()=>{fetch(`${API}/lms/catalog`).then(r=>r.ok?r.json():[]).then(setCourses).catch(()=>setCourses([])).finally(()=>setLoading(false))},[]);if(loading)return <div className="rounded-2xl border border-slate-200 bg-white p-8 text-center text-slate-500">Loading academy courses...</div>;if(!courses.length)return <div className="rounded-2xl border border-dashed border-slate-300 bg-white p-10 text-center"><BookOpen className="mx-auto text-blue-600"/><h2 className="mt-3 text-lg font-bold text-slate-950">Courses are being prepared</h2><p className="mt-2 text-sm text-slate-600">Published courses created by the Intellix LMS team will appear here automatically.</p></div>;return <div className="grid gap-5 md:grid-cols-2 lg:grid-cols-3">{courses.map(c=><Link key={c.CourseID} href={`/academy/${c.Slug}`} className="group rounded-2xl border border-slate-200 bg-white p-6 shadow-sm transition hover:-translate-y-1 hover:border-blue-300 hover:shadow-lg"><div className="flex items-start justify-between"><div className="rounded-xl bg-blue-50 p-3 text-blue-700"><BookOpen/></div><ArrowUpRight className="text-slate-400 group-hover:text-blue-700"/></div><h2 className="mt-5 text-lg font-black text-slate-950">{c.Title}</h2><p className="mt-2 line-clamp-3 text-sm leading-6 text-slate-600">{c.ShortDescription||c.Description||"Practical Intellix Academy course."}</p><div className="mt-5 flex items-center justify-between text-xs font-bold uppercase tracking-wide"><span className="text-blue-700">{c.Level}</span><span className="text-slate-500">{Number(c.Price||0)===0?"Free":`PKR ${Number(c.Price).toLocaleString()}`}</span></div></Link>)}</div>}
