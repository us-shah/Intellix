"use client";
import {useEffect,useState} from "react"; import LmsShell from "@/components/lms/LmsShell"; import {Card,PageTitle} from "@/components/lms/Ui"; import {lmsApi} from "@/lib/lms";
export default function Page(){const [s,setS]=useState({courses:0,enrollments:0,assignments:0,submissions:0});useEffect(()=>{Promise.all([lmsApi.courses(),lmsApi.enrollments(),lmsApi.assignments(),lmsApi.submissions()]).then(([a,b,c,d])=>setS({courses:a.length,enrollments:b.length,assignments:c.length,submissions:d.length}))},[]);return <LmsShell mode="admin"><PageTitle title="LMS Overview" description="Manage Intellix Academy from one place"/><div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{Object.entries(s).map(([k,v])=><Card key={k}><p className="capitalize text-slate-500">{k}</p><p className="mt-2 text-3xl font-bold">{v}</p></Card>)}</div></LmsShell>}
