"use client";
import { FormEvent, useState } from "react";
import { api } from "@/lib/auth";

export default function ChangePasswordPage(){const[current,setCurrent]=useState("");const[next,setNext]=useState("");const[confirm,setConfirm]=useState("");const[message,setMessage]=useState("");const[error,setError]=useState("");async function submit(e:FormEvent){e.preventDefault();setError("");setMessage("");if(next!==confirm){setError("Passwords do not match");return;}try{const{data}=await api.put("/auth/change-password",{CurrentPassword:current,NewPassword:next});setMessage(data.message);setCurrent("");setNext("");setConfirm("");}catch(err:any){setError(err.response?.data?.detail||"Unable to change password");}}return <div className="mx-auto max-w-xl p-6"><h1 className="text-2xl font-bold">Change password</h1><form onSubmit={submit} className="mt-6 space-y-4 rounded-xl bg-white p-6 shadow">{message&&<p className="rounded bg-green-50 p-3 text-green-700">{message}</p>}{error&&<p className="rounded bg-red-50 p-3 text-red-700">{error}</p>}<input className="w-full rounded border p-3" type="password" placeholder="Current password" value={current} onChange={e=>setCurrent(e.target.value)} required/><input className="w-full rounded border p-3" type="password" minLength={8} placeholder="New password" value={next} onChange={e=>setNext(e.target.value)} required/><input className="w-full rounded border p-3" type="password" minLength={8} placeholder="Confirm new password" value={confirm} onChange={e=>setConfirm(e.target.value)} required/><button className="rounded bg-blue-600 px-5 py-3 font-semibold text-white">Update password</button></form></div>}
