"use client";import{useEffect,useState}from"react";import Link from"next/link";import LmsShell from"@/components/lms/LmsShell";import{Card,Empty,PageTitle}from"@/components/lms/Ui";import{lmsApi}from"@/lib/lms";import type{Course}from"@/types/lms";
export default function Page(){const[x,setX]=useState<Course[]>([]);useEffect(()=>{lmsApi.courses().then(setX)},[]);return <LmsShell mode="instructor"><PageTitle title="Courses"/><div className="grid gap-4 md:grid-cols-2">{x.map(c=><Card key={c.CourseID}><h2 className="font-semibold">{c.Title}</h2><p className="mt-2 text-sm text-slate-500">{c.ShortDescription}</p><Link className="mt-4 inline-block text-blue-600" href={`/dashboard/lms/courses/${c.CourseID}/lessons`}>Manage lessons</Link></Card>)}</div>{!x.length&&<Empty text="No courses available."/>}</LmsShell>}
