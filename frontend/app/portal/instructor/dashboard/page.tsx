"use client";import{useEffect,useState}from"react";import LmsShell from"@/components/lms/LmsShell";import{Card,PageTitle}from"@/components/lms/Ui";import{lmsApi}from"@/lib/lms";
export default function Page(){const[s,setS]=useState({courses:0,assignments:0,submissions:0,pending:0});useEffect(()=>{Promise.all([lmsApi.courses(),lmsApi.assignments(),lmsApi.submissions()]).then(([c,a,x])=>setS({courses:c.length,assignments:a.length,submissions:x.length,pending:x.filter(i=>i.Status!=="graded").length}))},[]);return <LmsShell mode="instructor"><PageTitle title="Instructor dashboard"/><div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{Object.entries(s).map(([k,v])=><Card key={k}><p className="capitalize text-slate-500">{k}</p><p className="mt-2 text-3xl font-bold">{v}</p></Card>)}</div></LmsShell>}
